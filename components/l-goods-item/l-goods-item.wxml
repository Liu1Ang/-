<!--components/l-goods-item/l-goods-item.wxml-->
<view class="goods-item" bindtap="getgoods">
  <image class="img" lazy-load src="{{item.show.img||item.image||item.img}}" mode="widthFix"/>
  <view class="desc-info">
    <view class="title">{{item.title}}</view>
    <view class="info">
      <text class="price">￥{{item.price}}</text>
      <view>
        <image class="icon" src="/assets/common/favor.png"/>
        <text class="cfav">{{item.cfav}}</text>
      </view>
    </view>
  </view>
</view>
